language: python

# build_image: shippableimages/ubuntu1204_python

python:
  - 2.6
  - 2.7
  - 3.2
  - 3.3
  - 3.4

before_install:
  - env
  # In order to install matplotlib
  - sudo apt-get install -y python${VIRTUALENV_PYTHON}-dev 

  # set up a virtualenv and activate the python version that you want to use
  - mkdir -p $HOME/bldve/
  - virtualenv -p $SHIPPABLE_PYTHON  $HOME/bldve/
  - source $HOME/bldve/bin/activate

install:
  - pip install numpy nose coverage
  - python setup.py install

before_script:
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage

script:
  - nosetests --with-coverage --cover-package=astronomia --with-doctest --with-xunit --xunit-file=shippable/testresults/nosetests.xml
  - coverage xml --include="ts*/*" --omit="tests/*" --omit=".tox/*" -o shippable/codecoverage/coverage.xml 

cache: true
